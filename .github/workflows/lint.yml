name: Lint

on: [pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      black_args: "--check"
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: psf/black@stable
        with:
          args: ". --check"
